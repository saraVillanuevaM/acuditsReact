export default () => {

    return (
        <div>
            
           <h1> An joke a day keeps the doctor away</h1>
 <button id="chiste">Next Joke</button>

        </div>
    );
};


// //  document.querySelector('#chiste').addEventListener ('click', () =>{
//             fetch('https://icanhazdadjoke.com/slack')
//             .then(res => res.json())
//             .then(res=> document.querySelector('h3').textContent= res.attachments[0].text); 
//         });
        